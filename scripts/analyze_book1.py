import pandas as pd\nimport os\n\ndef analyze_excel(file_path):\n    """\n    Analyzes the structure of an Excel file and converts it to CSV format.\n\tArgs:\n\t\tfile_path (str): Path to the input Excel file.\n\n    Returns:\n\t\tNone\n    """\n    # Check if the file exists\n    if not os.path.exists(file_path):\n        print(f'File not found: {file_path}')\n        return\n\n    # Read the Excel file\n    try:\n        xls = pd.ExcelFile(file_path)\n    except Exception as e:\n        print(f'Failed to read Excel file: {e}')\n        return\n\n    # Loop through each sheet and convert to CSV\n    for sheet_name in xls.sheet_names:\n        df = pd.read_excel(xls, sheet_name=sheet_name)\n        csv_file_path = f'{sheet_name}.csv'\n        df.to_csv(csv_file_path, index=False)\n        print(f'Converted {sheet_name} to {csv_file_path}')\n\n# Example usage\n# analyze_excel('path_to_excel_file.xlsx')\n